{
  "name": "ridge-nedb",
  "version": "1.1.4",
  "description": "NeDB文档存储库",
  "main": "src/index.js",
  "scripts": {
    "start": "node ./test/start.js",
    "debug:test": "set DEBUG=wind:* && jest --verbose --no-cache",
    "test": "jest --verbose --no-cache",
    "testad": "jest ./test/admin.test.js --verbose --no-cache",
    "coverage": "jest --coverage",
    "build": "babel src -d build"
  },
  "license": "ISC",
  "dependencies": {
    "glob": "^7.2.0",
    "nedb": "^1.8.0"
  },
  "ava": {
    "files": [
      "test/*.test.js"
    ]
  }
}