{
  "name": "ridge-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "patch-package",
    "server": "npm run start --workspace=ridge-cloud-server"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "patch-package": "^8.0.0"
  },
  "author": "LiuHan"
}
